{
  "name": "${{ values.name }}",
  "version": "0.1.0",
  "main": "index.js",
  "scripts": {
    "start": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "${{ values.owner }}",
  "license": "MIT",
  "description": "A sample Node.js backend",
  "dependencies": {
    "express": "^5.1.0"
  }
}
